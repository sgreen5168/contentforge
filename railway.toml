[build]
builder = "dockerfile"
dockerfilePath = "video-engine-only (1)/video-engine/backend/Dockerfile"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
